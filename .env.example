# Only needed for local dev against the standalone server/ (not needed on
# Vercel — frontend and /api are the same deployment there, same-origin
# is used automatically).
# VITE_API_BASE_URL=http://localhost:4000
