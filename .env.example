# Copy this file to .env and fill in the real value (never commit .env itself).
# Points the frontend at the standalone backend in server/ — see server/README.md
# for how to run it. During local dev this is usually http://localhost:4000.
VITE_API_BASE_URL=http://localhost:4000
