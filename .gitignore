# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (Supabase keys, etc.) — never commit these
.env
.env.*
!.env.example

# Standalone server: its own env
server/.env
server/node_modules

# Local SQLite data file (repo root, shared by api/ and server/) — never commit real data
data/*.db
data/*.db-*
!data/.gitkeep

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
